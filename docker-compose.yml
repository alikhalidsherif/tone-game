version: '3.8'

services:
  tonematch:
    build: .
    ports:
      - "8080:3001"
    volumes:
      - tonematch-data:/app/data
    restart: unless-stopped
    container_name: tonematch-game

volumes:
  tonematch-data: